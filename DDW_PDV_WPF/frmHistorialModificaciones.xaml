<Page x:Class="DDW_PDV_WPF.frmHistorialModificaciones"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DDW_PDV_WPF"
      mc:Ignorable="d" 
      Title="frmHistorialModificaciones">

    <Grid Background="#dedede">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Panel Izquierdo -->
        <StackPanel Margin="20" Grid.Column="0">
            <TextBlock Text="HISTORIAL DE MODIFICACIONES A INVENTARIO" FontSize="18" FontWeight="Bold"/>
            <Border Grid.Row="0" BorderBrush="#FFABAdB3" BorderThickness="1" 
        Background="White" CornerRadius="3" Margin="10,0,10,10">
                <Grid>
                    <TextBox x:Name="txtBusqueda"
                 Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}"
                 Background="Transparent"
                 BorderThickness="0"
                 Padding="10"
                 FontSize="14"
                 VerticalAlignment="Center"
                 Foreground="Black"/>

                    <TextBlock Text="Buscar artículos..."
                   Foreground="Gray"
                   FontStyle="Italic"
                   FontSize="14"
                   Padding="15,0,0,0"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Left"
                   />

                    <Button x:Name="btnClearSearch"
                Content="✕"
                FontSize="12"
                Foreground="Gray"
                Background="Transparent"
                BorderThickness="0"
                Padding="0"
                Width="20"
                Height="20"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Margin="0,0,10,0"
                />
                </Grid>
            </Border>

            <DataGrid AutoGenerateColumns="False" Height="400" Background="#dedede">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="FECHA Y HORA" Binding="{Binding FechaHora}" Width="150"/>
                    <DataGridTextColumn Header="USUARIO" Binding="{Binding Usuario}" Width="100"/>
                    <DataGridTextColumn Header="TIPO DE MODIFICACIÓN" Binding="{Binding TipoModificacion}" Width="120"/>
                    <DataGridTextColumn Header="PRODUCTOS" Binding="{Binding Producto}" Width="120"/>
                    <DataGridTextColumn Header="MOTIVO" Binding="{Binding Motivo}" Width="180"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <!-- Panel Derecho -->
        <Grid Grid.Column="1" Background="#E5E5E5" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="DETALLES DE LA MODIFICACIÓN" FontSize="22" FontWeight="Bold" Foreground="Gray" HorizontalAlignment="Center" Grid.Row="0" Margin="0,0,0,10"/>

            <StackPanel Grid.Row="1" Margin="10">
                <TextBlock Text="FECHA Y HORA" FontWeight="Bold"/>
                <TextBlock Name="txtFechaHora" FontSize="14" Foreground="#333"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="10">
                <TextBlock Text="USUARIO" FontWeight="Bold"/>
                <TextBlock Name="txtUsuario" FontSize="14" Foreground="#333"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Margin="10">
                <TextBlock Text="TIPO DE MODIFICACIÓN" FontWeight="Bold"/>
                <TextBlock Name="txtTipoModificacion" FontSize="14" Foreground="#333"/>
            </StackPanel>

            <StackPanel Grid.Row="4" Margin="10">
                <TextBlock Text="PRODUCTOS" FontWeight="Bold"/>
                <Image Name="imgProducto"  Stretch="Uniform" Margin="10" HorizontalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Row="5" Margin="10">
                <TextBlock Text="Motivo:" FontWeight="Bold"/>
                <TextBlock Name="txtMotivo" TextWrapping="Wrap" FontSize="14" Foreground="#333"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
