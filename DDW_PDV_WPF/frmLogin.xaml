<Window x:Class="DDW_PDV_WPF.frmLogin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DDW_PDV_WPF"
        mc:Ignorable="d"
        Icon="/Resources/omegasymbol.png"
        Title="Iniciar Sesión" 
        Height="450" Width="300"
        MinWidth="280" MaxWidth="350"
        WindowStartupLocation="CenterScreen" 
        ResizeMode="NoResize" 
        Background="#2E5E4E" 
        WindowStyle="None">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#ffff"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <Style TargetType="Button" x:Key="CloseButtonStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="0,5,5,0"/>
        </Style>

        <Style TargetType="Button" x:Key="LoginButtonStyle">
            <Setter Property="Background" Value="#6B9D89"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="Height" Value="40"/>

        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Botón de cerrar -->
        <Button Content="✖" 
                Style="{StaticResource CloseButtonStyle}"
                Click="Cerrar_Click"
                Grid.Row="0"/>

        <!-- Contenido principal -->
        <StackPanel Grid.Row="1"
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center" 
                    Width="220">

            <!-- Avatar -->
            <Grid HorizontalAlignment="Center" Margin="0,0,0,20">
                <Ellipse Width="120" Height="120" Fill="#ffff">
                    <Ellipse.Stroke>
                        <SolidColorBrush Color="#CCCCCC" Opacity="0.5"/>
                    </Ellipse.Stroke>
                </Ellipse>
                <Image Source="/Resources/omegasymbol.png" Margin="62,33,62,34"/>
            </Grid>

            <!-- Campos de formulario -->
            <TextBlock Text="Usuario" Margin="0,0,0,5" FontSize="20"/>
            <TextBox x:Name="txtUsuario" Text="" FontSize="20"/>

            <TextBlock Text="Contraseña" Margin="0,10,0,5" FontSize="20"/>
            <PasswordBox x:Name="txtContrasena" Password="" FontSize="20"/>

            <!-- Botón de login -->
            <Button x:Name="btnIniciarSesion"
                    Content="Iniciar Sesión" 
                    Style="{StaticResource LoginButtonStyle}"
                    Click="Button_Click"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
